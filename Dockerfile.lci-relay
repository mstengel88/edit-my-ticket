FROM node:20-alpine

WORKDIR /app

RUN npm init -y \
  && npm pkg set type=module \
  && npm install ws

COPY scripts/loadrite-lci-relay.mjs ./scripts/loadrite-lci-relay.mjs

CMD ["node", "scripts/loadrite-lci-relay.mjs"]
